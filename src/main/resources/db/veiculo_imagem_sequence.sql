-- Script SQL para criar a sequence necessária para a tabela VEICULO_IMAGEM
-- Execute este script no banco de dados Firebird antes de usar o sistema

-- Criar sequence para ID da tabela VEICULO_IMAGEM
CREATE SEQUENCE GT_VEICULO_IMAGEM_ID;

-- Definir valor inicial (opcional, ajuste conforme necessário)
-- ALTER SEQUENCE GT_VEICULO_IMAGEM_ID RESTART WITH 1;
